# Vault home task
The app is built with Vite + React + Typescript + Chakra with 
tests on Vitest and React Testing Library

To run the app use:
```shell
yarn dev
```

To run tests use:
```shell
yarn test
```